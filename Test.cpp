#include"vector.h"
#include<iostream>

int main()
{
	/*allocator<int> give;
	int* a = give.allocate(10);
	for (int i = 0; i < 10; i++)
	{
		give.construct(a+i,i);
	}
	for (int i = 0; i < 10; i++)
	{
		std::cout << a[i] << std::endl;
	}
	give.destory(a);
	give.deallocate(a);*/

	return 0;
}